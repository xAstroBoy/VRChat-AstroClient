namespace AstroClient.Kaned
{
    internal class RotationFix : AstroEvents
    {
        //internal override void OnUpdate()
        //{
        //    //do 2 things here
        //    //1. Apply the inverse of the rotation that vrc applies to cancel it out
        //    //Note for 1 if this is run in lateupdate then should be able to just reset the rotation then run the deltatime rotation
        //    //2. Apply the deltaTime rotation
        //}
    }
}
